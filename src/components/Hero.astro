<div class="hero">
  <p class="eyebrow">Software a la medida&nbsp;&nbsp;·&nbsp;&nbsp;<span>Desde $2,500 MXN/mes</span></p>
  <h1>Tu negocio merece<br />su propia plataforma.<br /><em>Sin pagar una fortuna.</em></h1>
  <p class="hero-sub">
    Construimos la app de tu negocio en 4 semanas, sin costo inicial.
    Software que se adapta a ti, no al revés.
  </p>
  <div class="cta-row">
    <a href="#contacto" class="btn-primary cta-btn">Agenda una demo gratis</a>
    <a href="#como-funciona" class="btn-secondary cta-btn">Ver cómo funciona ↓</a>
  </div>
</div>

<style>
  .hero {
    max-width: 720px;
    margin: 0 auto;
    padding: 88px 24px 72px;
    text-align: center;
  }

  .eyebrow {
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    opacity: 0.45;
    margin-bottom: 24px;
  }

  .eyebrow span {
    color: var(--color-aqua);
    opacity: 1;
  }

  h1 {
    font-family: var(--font-display);
    font-size: clamp(40px, 6vw, 64px);
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -1.5px;
    color: var(--color-carbon);
    margin-bottom: 24px;
  }

  h1 em {
    font-style: italic;
    font-weight: 300;
  }

  .hero-sub {
    font-size: 18px;
    line-height: 1.65;
    opacity: 0.72;
    max-width: 500px;
    margin: 0 auto 40px;
  }

  .cta-row {
    display: flex;
    gap: 12px;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 80px;
  }

  .cta-btn {
    font-size: 16px;
    padding: 14px 28px;
    border-radius: 8px;
  }

  @media (max-width: 600px) {
    .hero {
      padding: 64px 20px 56px;
    }
  }
</style>
